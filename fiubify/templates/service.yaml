{{- range .Values.internalServices}}
---
apiVersion: v1
kind: Service
metadata:
  name: service-{{ .serviceName}}
spec:
  type: ClusterIP
  ports:
    - port: {{ .port}}
      protocol: TCP
      targetPort: {{ .port}}
  selector:
    service: {{ .serviceName }}

{{- end}}
